apiVersion: v1
kind: ConfigMap
metadata:
  name: ops-agent-config
data:
  REDIS_URL: redis://redis-svc:6379/0
  QUEUE_MODE: rq
  EXECUTION_MODE: rq
  DIAGNOSIS_PROVIDER: rules
  AGENT_CORE_ENABLED: "true"
  AGENT_REASONER_PROVIDER: rules
  AGENT_MODEL_CONTEXT_WINDOW: "0"
  AGENT_MODEL_INPUT_LIMIT: "12000"
  AGENT_MODEL_OUTPUT_TOKENS: "1000"
  AGENT_MODEL_SAFETY_MARGIN: "512"
  AGENT_EVIDENCE_TTLS: "{}"
  PROMETHEUS_URL: http://kube-prometheus-stack-prometheus.monitoring.svc:9090
  OUTBOX_POLL_SECONDS: "1"
  AUTO_REMEDIATE_ENABLED: "false"
  ALLOWED_NAMESPACES: default
  REQUIRE_OPERATOR_AUTH: "true"
